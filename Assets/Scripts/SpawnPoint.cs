using UnityEngine;

/// <summary>
/// Simple marker script for spawn points. No logic - just used for tagging.
/// </summary>
public class SpawnPoint : MonoBehaviour
{
    // Empty MonoBehaviour - just used as a marker/tag
}
